# AGENTS

## Non-negotiables

- 実行用コードは `scripts/` に置く
- 加工済みデータや出所メモは `data/` に置く
- 図表や出力ファイルは `output/` に置く
- Word 本文はルートの `thesis.docx` または `report.docx` を使う
- 生データの保存先は `.env` で管理する
- `.env` と生データは commit しない
- スクリプトは repo ルートで実行する前提で、相対パスを優先する
- フォルダ構成は必要最小限に保ち、むやみに細分化しない
